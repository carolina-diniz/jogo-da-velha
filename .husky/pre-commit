branch=$(git branch --show-current); if [ "$branch" = "main" ]; then echo '🚫 Commit direto na main proibido!'; exit 1; fi

# Format and fix code before commit
pnpm pretty-quick --staged
pnpm run lint
pnpm run ts-check
