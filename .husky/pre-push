branch=$(git branch --show-current); if [ "$branch" = "main" ]; then echo '🚫 Push direto na main proibido!'; exit 1; fi

# Run checks on all files before push
npm run prettier:check
npm run ts-check